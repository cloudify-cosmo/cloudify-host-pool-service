node_types:

  cloudify.nodes.HostPoolService:
    derived_from: cloudify.nodes.SoftwareComponent
    properties:
      pool:
        type: string
        description: Path to the service configuration file (YAML)
      port:
        type: integer
        description: Port to run the service on
        default: 8080
      debug:
        type: boolean
        description: Enable debug logging of the deployment
        default: false
      gunicorn_debug:
        type: boolean
        description: Enable debug logging of the Gunicorn server
        default: false
      source:
        type: string
        description: URL to the source code of the service
        default: https://github.com/01000101/cloudify-host-pool-service/archive/CFY-4940.zip
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: script.script_runner.tasks.run
          inputs:
            script_path:
              default: https://raw.githubusercontent.com/01000101/cloudify-host-pool-service/CFY-4940/scripts/create.py
        configure:
          implementation: script.script_runner.tasks.run
          inputs:
            script_path:
              default: https://raw.githubusercontent.com/01000101/cloudify-host-pool-service/CFY-4940/scripts/configure.py
        start:
          implementation: script.script_runner.tasks.run
          inputs:
            script_path:
              default: https://raw.githubusercontent.com/01000101/cloudify-host-pool-service/CFY-4940/scripts/start.py
        stop:
          implementation: script.script_runner.tasks.run
          inputs:
            script_path:
              default: https://raw.githubusercontent.com/01000101/cloudify-host-pool-service/CFY-4940/scripts/stop.py
        delete:
          implementation: script.script_runner.tasks.run
          inputs:
            script_path:
              default: https://raw.githubusercontent.com/01000101/cloudify-host-pool-service/CFY-4940/scripts/delete.py
